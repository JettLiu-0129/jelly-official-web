import MacScreenShot from "@/components/MacScreenShot";

# 配置服务提供商

Jelly 支持多种服务的配置，您可以根据需求自由选择您喜欢的服务。

<MacScreenShot src="/service-provider-settings.png" />

> 服务提供商配置界面

## 快速开始

如果您是首次使用 Jelly，想快速体验 AI 功能，可以粘贴下面的 API Key 到应用中。

:::danger
此为开发者个人的 API Key，不保证一定可用。为了您能够稳定使用，强烈建议您申请自己的 API Key。目前有多家模型提供商都提供完全免费的 API Key，足以应对个人日常使用。

此 API Key 下没有任何额度，只能调用免费模型。请不要用于违法用途。
:::

```text title="硅基流动"
sk-iyfhfevozacaszfqkhjjsvfdguzoqeysfcahyudvnckejsqo
```

```text title="智谱 AI"
1b1afb07014c4bf1933cdb68471033ae.bkr6ESafzTIQDGRS
```

API Key 申请，请查看[配置大模型服务](/guide/basic/service-config/llm)

## 列表顺序说明

:::warning 注意
请仔细阅读本部分，避免因意外调整顺序，导致工具调用错模型，给您造成损失。
:::

### 大模型服务列表

在 Jelly 中，可能存在大量 AI 工具同时调用同一模型的情况。为避免您更换模型时，需要将所有工具的“服务提供商”字段全都重新配置一遍，Jelly 的工具系统使用“标签”来寻找要调用的模型。

模型的标签是其在列表中的顺序决定的，第一个固定是“首选模型”，第二个固定是“备选模型”，以此类推。

所有 AI 工具默认调用“首选模型”。

因此，在默认情况下，如果您要一口气更换所有工具调用的模型，只需将目标模型拖到列表第一个。

:::tip 使用技巧 💡
您可以将免费模型放在列表第一个，作为“首选模型”，让大部分工具调用。将付费模型放在列表第二个，作为“备选模型”，让需要更高质量回答的 AI 工具调用。
:::

### 翻译服务列表

翻译服务虽然不存在这个问题，但是为了复用代码，也沿用了大模型服务列表的设计（开发者偷懒了=\_=）。

### 语音合成服务列表

语音合成服务列表没有使用这个“标签”的概念。详情请见[语音合成服务](/guide/basic/service-config/tts)。

## 从插件添加服务

Jelly 也支持从插件添加服务。

您可从[插件市场](/plugin/market)浏览可用的插件。

## 反馈

如果您找不到喜欢的服务，欢迎通过[反馈页面](/about/contact)告诉我们！
